spring:
  application:
    name: prescricao-api
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
header:
  login: xto-xtz
prescription:
  status:
    waiting: 0
    approved: 1
    rejected: 2
